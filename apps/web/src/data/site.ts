/**
 * Facts about the site itself that more than one page states. Keep them true:
 * the repository is public, so its issue tracker is a real contact route.
 */

export const siteName = "MathMyRate";

export const repositoryUrl = "https://github.com/mehedihasan2810/MathMyRate";

export const issuesUrl = `${repositoryUrl}/issues`;
