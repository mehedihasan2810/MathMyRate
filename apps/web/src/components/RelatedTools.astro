---
import { findHub, relatedTools, type ToolId } from "../data/tools";

interface Props {
  current: ToolId;
}

const related = relatedTools(Astro.props.current);
---

<section aria-labelledby="related-calculators" class="no-print mt-14 border-t border-line pt-10">
  <h2 id="related-calculators" class="display text-3xl font-bold">Related calculators</h2>
  <ul class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
    {related.map((tool) => (
      <li>
        <a href={tool.href} class="block h-full rounded-2xl border border-line bg-white p-5 transition-colors hover:border-ink/30">
          <span class="eyebrow block">{findHub(tool.hub).name}</span>
          <span class="display mt-2 block text-lg font-bold text-ink">{tool.name}</span>
          <span class="mt-1 block text-sm leading-relaxed text-ink/65">{tool.summary}</span>
        </a>
      </li>
    ))}
  </ul>
</section>
